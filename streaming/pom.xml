<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://maven.apache.org/POM/4.0.0"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>tv.okko.common.mp4</groupId>
        <artifactId>mp4parser-project</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>streaming</artifactId>
    <dependencies>
        <dependency>
            <groupId>tv.okko.common.mp4</groupId>
            <artifactId>isoparser</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>tv.okko.common.mp4</groupId>
            <artifactId>muxer</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <name>Stream Muxer</name>
    <description>This package has a focus on streams. It can read A/V data from e.g. a network source.</description>
    <url>https://github.com/sannies/mp4parser</url>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>